# @frwd/publisher

Emits `report.frwd.html` from a `.frwd` document, embedding the trusted standard
runtime from `@frwd/runtime`.

The result is a single portable HTML file that opens in any modern browser,
works fully offline, and stays readable with JavaScript disabled.

Framework-agnostic.

Not implemented yet — see task `t-006`.
