{
  "title": "Missing doctype",
  "description": "No <!doctype html>, so a browser renders this in quirks mode. FRWD depends on predictable standards-mode rendering, so this does not conform even though the content is entirely recoverable.",
  "conforming": false,
  "canonical": true,
  "expectedDiagnostics": [
    "missing-doctype"
  ]
}
